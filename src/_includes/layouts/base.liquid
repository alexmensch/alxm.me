<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="generator" content="{{ eleventy.generator }}">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="/assets/css/main.css">
    <link rel="preconnect" href="https://rsms.me/">
    <link rel="stylesheet" href="https://rsms.me/inter/inter.css">
</head>
<body class="{{ title | downcase }}">
    <div class="container-grid">
        <!-- Upper & lower divs must appear first in container-grid to ensure correct layering -->
        <div class="upper {{ title | downcase }}"></div>
        <div class="lower {{ title | downcase }}"></div>
        <!-- ^^^ -->
        <div class="navigation">
            <ul>
                {% assign rotatedNavigation = navigation.items | rotateCollection: navIndex %}
                {% for item in rotatedNavigation %}
                    {% unless item.hidden %}
                      <li><a href="{{ item.url }}">{{ item.title }}</a></li>
                    {% endunless %}
                    {% if collapseNav %}
                        {% break %}
                    {% endif %}
                {% endfor %}
            </ul>
        </div>
        {{ content }}
        <div class="footer">
            <span>Copyright &copy; {{ currentYear }} Alex Marshall <<a href="mailto:">hello [at] alxm.me</a>></span>
        </div>
    </div>
</body>
</html>
